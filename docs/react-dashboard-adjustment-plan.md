# React 资金看板调整开发计划

## 目标范围

本次调整围绕 React 看板页面进行，重点处理左侧入口、中间模块留白/保留、XRepo 背面历史走势、大行价格翻转图表、匿名成交昨日对比、机构分期限统计中间化与弹窗化、个人 QT 行情融合正回购和逆回购，以及正/逆回购需求合并。

主要修改文件预计集中在：

- `src/app/App.tsx`
- 必要时补充 `src/data/*.ts` mock 数据
- 必要时拆分新的 React 子组件，降低 `App.tsx` 单文件复杂度

## 本轮新增待办与里程碑（2026-06-22）

本轮调整聚焦当前 React 看板的顶部情绪与广播、中部 tab 重组、左侧入口交互、右侧机构热度走势增强，以及整页白板主题切换。

### Todolist

- [x] 顶部 `泰康资金情绪` 改为“文字 + 数字 + 时间 + 状态文案（紧张 / 宽松 / 一般，带颜色）”。
- [x] 顶部中间滚动广播改为：
  `【资金交易提示】：早盘资金面XXX，存款类机构融出XXX，非银融出价格上升5-10bp。目前质押信用债的价格，隔夜R001+XXXbp，7天XXX%，14天XXX%，最新国际货币资金面情绪指数51。`
- [x] `大行价格` 从翻转效果改为弹窗。
- [x] `大行价格` 弹窗支持多选。
- [x] 保留左侧宽度小于阈值时的现有效果。
- [x] `XREPO` 对比维度新增“当天”。
- [x] `XREPO` 支持大弹窗查看。
- [x] 右侧 `机构热度走势` 增加“价格”折线图，形成 `价 / 笔数 / 量` 三指标。
- [x] 中间期限分组明确拆成 `1d / 7d / 14d / 21d / 其他`。
- [x] 删除 `已回复 / 未回复` 筛选项。
- [x] `资金缺口` 和 `在途指令` 合并到同一个 tab。
- [x] `资金分机构统计` 下移到中下区域的 tab。
- [x] 删除中上区域 `非银报价` 标题。
- [x] 右侧区域按“机构热度走势”口径统一整理标题、图例和 tooltip。
- [x] 全页切到白板主题。
- [x] 完成后运行 `npm run build` 并做关键交互回归。

### 里程碑

#### M1：顶部信息与基础口径收敛

目标：先把顶部和筛选口径调对，避免后续交互建立在错误文案和错误筛选上。

待办：

- [x] 完成顶部 `泰康资金情绪` 的文字、数字、时间和状态色表达。
- [x] 完成顶部滚动广播文案替换。
- [x] 删除中上区域 `非银报价` 标题。
- [x] 删除 `已回复 / 未回复` 筛选项。
- [x] 保留左侧窄宽度收缩效果。

验收：

- 顶部文案、状态和时间口径一致。
- 非银报价区域标题更简洁。
- 左侧窄栏行为不回退。

#### M2：弹窗与对比能力改造

目标：把原先翻转式入口改成更直观的弹窗交互，并补齐 XREPO 当天对比。

待办：

- [x] `大行价格` 改为弹窗交互。
- [x] `大行价格` 弹窗支持多选。
- [x] `XREPO` 增加“当天”对比维度。
- [x] `XREPO` 接入大弹窗。

验收：

- 页面不再依赖大行价格翻转交互。
- 大行价格和 XREPO 都可以稳定打开弹窗。
- `XREPO` 可以查看“当天 + 历史/对比”。

#### M3：中部布局重组

目标：把中部期限和底部 tab 重新梳理，突出交易员真正会切换的内容。

待办：

- [x] 中间期限改为 `1d / 7d / 14d / 21d / 其他`。
- [x] `资金缺口` 和 `在途指令` 合并到一个 tab。
- [x] `资金分机构统计` 移到中下 tab。

验收：

- 中部 tab 分工明确。
- 期限口径与表格/图表一致。
- 中下区域结构更贴近最新需求。

#### M4：右侧机构热度走势增强

目标：把右侧走势从单一热度表达提升为 `价 / 笔数 / 量` 的组合分析。

待办：

- [x] `机构热度走势` 增加价格折线图。
- [x] 形成 `价 / 笔数 / 量` 三指标联动展示。
- [x] 统一右侧区域标题、图例和 tooltip 口径。

验收：

- 右侧图表至少包含价格、笔数、量三类表达。
- 图例、单位和 tooltip 一致。

#### M5：白板主题与联调验收

目标：完成全页白板主题切换，并对主要交互做一次收口验证。

待办：

- [x] 整页切换为白板主题。
- [x] 检查表格、弹窗、tab、图表在白底下的层次。
- [x] 运行 `npm run build`。
- [x] 验证顶部广播、情绪条、大行价格弹窗、XREPO 弹窗、中部 tab、右侧走势。
- [x] 记录未完成项和残留问题。

### 本轮执行记录 R1（2026-06-23）

- 已完成 React 看板白板主题切换，新增 `src/styles/react-whiteboard.css` 并通过 `src/styles/index.css` 接入。
- 已将左侧 `大行价格`、`XREPO` 从翻转模式调整为表格选中 + 大弹窗/大图联动。
- 已补齐 `XREPO` 的”当天”范围。
- 已新增 `src/app/dashboardUtils.js` 与 `src/app/dashboardUtils.test.mjs`，覆盖期限标签、多选切换、`XREPO` 当天点位和情绪分级逻辑。
- 已新增 `openFrameById` 与 `LeftInfoColumn` 重构，统一弹窗打开方式。

### 本轮执行记录 R2（2026-06-23）

- **#1 泰康资金情绪**：`SentimentChipWithPopover` 改为”文字 + 数字 + 时间 + 状态色文字(紧张/宽松/一般)”，使用 `getSentimentState()` 驱动颜色。
- **#2 滚动广播**：`DEFAULT_TRADING_NOTICE_TEXT` 替换为【资金交易提示】指定格式，含隔夜/7天/14天利率及情绪指数。
- **#6 机构热度走势**：`BarometerMetric` 新增 `”price”` 选项，补充价格 mock 数据（利率折线），形成 `价 / 笔数 / 量` 三指标。价格模式下 y 轴自动适配小数精度，跳过机构因子缩放。
- **#7 期限显示**：期限 tab 按钮调用 `quoteTenorDisplayLabel()` 渲染为 `1d / 7d / 14d / 21d / 其他`。
- **#8 删除已回复/未回复**：移除报价筛选区的已回复/未回复 filter 按钮。
- **#11 删除非银报价标题**：移除中上区域”非银报价”标题文字。
- **#12 机构热度走势标题**：从”机构报价热度”改为”机构热度走势”。
- 更新总 Checklist 和里程碑 M1-M8 所有待办项为 `[x]`。
- 验证结果：`npm run test:react-dashboard` 5/5 通过，`vite build` 通过，浏览器无控制台错误。

验收：

- 页面主题统一。
- 无明显错位、遮挡、文字溢出。
- 构建通过，核心交互可操作。
- 13 项需求已完成/已验证。

## 参考图

### 大行定价走势

![大行定价走势](C:/Users/Administrator/AppData/Roaming/LarkShell/sdk_storage/e010695947f535d318bd7875951b705c/resources/images/img_v3_0212g_398e5446-8f7c-4e78-965a-e9683b44bc0g.jpg)

### 大行定价与加权价差

![大行定价与加权价差](C:/Users/Administrator/AppData/Roaming/LarkShell/sdk_storage/e010695947f535d318bd7875951b705c/resources/images/img_v3_0212g_7a50cffe-cf19-4a63-948f-32d64f99301g.jpg)

### 机构分期限统计大图

![机构分期限统计大图](C:/Users/Administrator/AppData/Roaming/LarkShell/sdk_storage/e010695947f535d318bd7875951b705c/resources/images/img_v3_0212g_cfb4aa5f-6197-435c-b8e1-8330eb29be0g.jpg)

## 需求拆解

### 1. 左侧入口与中间模块

- 左侧删除入口直接删除，不保留占位。
- 中间删除的内容保留空位。
- 中间的「匿名成交走势图」需要保留，不参与删除。
- 「历史成交走势」不再作为独立中间模块展示，迁移到 XRepo 的背面。

需要确认/保留的模块策略：

| 模块 | 左侧入口 | 中间内容 |
| --- | --- | --- |
| 行情摘要今日预览 | 删除 | 留空 |
| DR007 / 资金情绪 | 删除 | 留空 |
| 历史成交趋势 | 删除 | 移入 XRepo 背面 |
| 匿名成交走势图 | 删除入口 | 中间保留图表 |
| 个人 & 机构 | 删除 | 留空 |

### 2. XRepo 只展示 R001 相关数据

- XRepo 仅展示合约名称包含 `R001` 的数据。
- 保留 `R001-mini`、`DFR001-mini`、`CDR001-mini` 等包含 `R001` 的合约。
- 删除/隐藏不包含 `R001` 的期限或合约，例如 `R007`、`R014`、`R021`、`R028`。
- XRepo 摘要、表格、图表、筛选项统一按 `contractName.includes("R001")` 或等价规则过滤。

验收点：

- XRepo 表格中可以看到 `R001`、`R001-mini` 等。
- 不出现 `R007`、`R014` 等非 R001 合约。

### 3. XRepo 背面：历史成交走势

- 历史成交走势放到 XRepo 卡片背面。
- 点击 XRepo 某一行时触发翻转。
- 被点击行作为默认传入参数，决定背面历史走势展示的合约。
- 例如点击 `R001-mini`，背面默认展示 `R001-mini` 的历史成交趋势。
- 背面需要支持返回正面。
- 返回正面支持两种方式：
  - 再次点击背面空白区域返回。
  - 点击背面右上角的返回按钮返回。

建议状态设计：

```ts
const [flippedXrepoContract, setFlippedXrepoContract] = useState<string | null>(null);
```

交互：

```text
XRepo 正面表格行点击
  -> setFlippedXrepoContract(row.contractName)
  -> 卡片翻转
  -> 历史成交走势读取 flippedXrepoContract

XRepo 背面空白区域点击 / 返回按钮点击
  -> setFlippedXrepoContract(null)
  -> 卡片翻回正面
```

### 4. 匿名成交走势图增加昨日对比折线

- 保留现有匿名成交走势图。
- 在原有今日走势基础上叠加昨日对比折线。
- 今日与昨日需要在图例中明确区分。
- 建议：
  - 今日：实线
  - 昨日：虚线或低饱和度折线
- 若当前图中已有成交量柱状图，昨日对比只叠加利率折线，避免图面过载。

验收点：

- 图例可见「今日」「昨日」。
- 鼠标悬浮时 tooltip 同时显示今日/昨日数值。

### 5. 大行价格翻转图表

- 大行价格表格每一行支持点击。
- 点击某一银行行后，卡片翻转，展示该银行多日历史。
- 翻转背面按参考图分为两个图表：
  - 大行定价走势
  - 大行定价与加权价差
- 返回正面支持两种方式：
  - 再次点击背面空白区域返回。
  - 点击背面右上角的返回按钮返回。

#### 5.1 大行定价走势

参考图：大行定价走势。

图表内容：

- 折线 1：出给非银价格(%)
- 折线 2：出给银行价格(%)
- 柱状：非银-银行价差(BP)
- 双 Y 轴：
  - 左轴：利率(%)
  - 右轴：价差(BP)
- tooltip 显示日期、两条价格、价差。

#### 5.2 大行定价与加权价差

参考图：大行定价与加权价差。

图表内容：

- 上半区：给银行价差(BP)
- 下半区：给非银价差(BP)
- 两组柱状图上下排列。
- 支持 tooltip。

建议状态设计：

```ts
const [flippedBankId, setFlippedBankId] = useState<string | null>(null);
```

交互：

```text
大行价格表格行点击
  -> setFlippedBankId(row.institution)
  -> 卡片翻转
  -> 背面按 bankId 读取历史 mock 数据

大行价格背面空白区域点击 / 返回按钮点击
  -> setFlippedBankId(null)
  -> 卡片翻回正面
```

### 6. 机构分期限统计移动到中间，并支持入口弹窗

参考图：机构分期限统计大图。

目标：

- 将机构分期限统计移动到中间行情矩阵区，作为中间区核心统计模块。
- 中间区默认展示一个压缩版统计图/统计摘要。
- 左侧「机构分期限统计入口」保留为轻量入口，不直接铺开大图。
- 入口中的「期限」和「指标」改为下拉框展示。
- 点击入口或入口中的展开按钮后，打开机构分期限统计大弹窗。
- 大弹窗做成更接近参考图的密集统计视图。
- 顶部保留指标选择：
  - 加权利率
  - 交易额
  - 回购余额
  - 跨月金额
  - 净融入金额
- 机构类型通过 checkbox 控制显示。
- 期限通过 checkbox 控制显示。
- 展示模式支持：
  - 正回购 / 逆回购
  - 折线图 / 柱状图
  - 期限 / 机构
- 默认可优先落地为柱状堆叠图。

交互调整：

- 图例点击仍保留筛选/高亮。
- 非图例项点击不再直接切换图表。
- 非图例项点击改为下拉框展开明细。
- 下拉明细展示该项对应的机构、期限、指标值、同比/环比或更新时间。
- 入口区的期限下拉框只控制入口摘要和弹窗默认参数。
- 入口区的指标下拉框只控制入口摘要和弹窗默认参数。
- 大弹窗内部仍保留完整筛选能力。

### 7. 个人 QT 行情融合正回购和逆回购

- 「个人 QT 行情」需要同时融合正回购和逆回购两侧数据。
- 卡片标题建议保留「个人 QT 行情」，副标题或标签体现「正回购 / 逆回购」。
- 在卡片顶部增加「分机构统计」下拉框，并保留正回购/逆回购口径切换或汇总口径。
- 下拉选项建议：
  - 全部机构
  - 大型银行
  - 大型商业/政策性银行
  - 股份制商业银行
  - 中小型银行
  - 城市商业银行
  - 农村金融机构
  - 证券公司
  - 保险公司
  - 基金公司及产品
  - 货币市场基金
  - 理财子公司及理财类产品
  - 其他产品类
  - 其他
- 选择后，正回购和逆回购两侧数据同步按机构类型过滤或聚合。
- 图表/表格需同时表达：
  - 正回购 QT 行情
  - 逆回购 QT 行情
  - 正回购与逆回购价差/利差
  - 样本量或覆盖机构数
- 若空间不足，默认展示融合摘要 + 趋势线，详细表格进入下拉或弹层。

### 8. 正/逆回购需求合并

按已确认线框实现为单表：

```text
┌────────────────────────────────────────────────────────────┐
│ 正/逆回购需求                                      [R001] [R007] │
│ 本方需求 · 押券 × 期限 · 单位：亿                              │
├────────────────────────────────────────────────────────────┤
│  ┌────────────┬──────────────┬──────────────┬──────────────┐ │
│  │ 正回购需求   │ R001          │ R007          │ 合计          │ │
│  ├────────────┼──────────────┼──────────────┼──────────────┤ │
│  │ 利率地方     │ 40.6 / 31.2  │ 16.4 / 13.1  │ 57.0 / 44.3  │ │
│  │ 存单商金     │ 0.0 / 0.0    │ 25.3 / 18.4  │ 25.3 / 18.4  │ │
│  │ 信用        │ 9.2 / 7.1    │ 15.7 / 15.7  │ 24.9 / 22.8  │ │
│  ├────────────┼──────────────┼──────────────┼──────────────┤ │
│  │ 合计        │ 49.8 / 38.3  │ 57.4 / 47.2  │ 107.2 / 85.5 │ │
│  ├────────────┼──────────────┼──────────────┼──────────────┤ │
│  │ 逆回购需求   │ 35.7 / 29.4  │ 48.3 / 35.1  │ 84.0 / 64.5  │ │
│  └────────────┴──────────────┴──────────────┴──────────────┘ │
└────────────────────────────────────────────────────────────┘
```

规则：

- 正回购需求保留押券分类明细。
- 逆回购需求只保留汇总行。
- 单元格 = 需求量 / 已完成量。

## 实施步骤

### Step 1：模块入口与布局调整

- 删除左侧指定入口。
- 中间删除模块改为空位。
- 保留匿名成交走势图。
- 确认页面布局不塌陷。

### Step 2：XRepo R001 过滤与背面历史走势

- 增加 XRepo 合约过滤逻辑。
- 保留包含 `R001` 的合约名称。
- 增加 XRepo 行点击翻转。
- 接入历史成交走势组件。
- 背面支持空白区域点击返回和返回按钮返回。

### Step 3：匿名成交走势图昨日对比

- 增加昨日 mock 序列。
- 图表叠加昨日折线。
- 增加图例与 tooltip。

### Step 4：大行价格翻转图表

- 增加大行价格行点击。
- 增加银行历史 mock 数据。
- 实现两个背面图表。
- 增加返回正面交互：空白区域点击返回 + 返回按钮返回。

### Step 5：机构分期限统计中间化与弹窗

- 将机构分期限统计移动到中间行情矩阵区。
- 左侧入口改为轻量入口。
- 入口期限与指标改为下拉框。
- 点击入口打开大弹窗。
- 大弹窗内重构筛选区。
- 增加 checkbox 组选项。
- 非图例点击改为下拉展开。
- 优先落地柱状堆叠图。

### Step 6：个人 QT 行情融合正回购和逆回购

- 融合正回购和逆回购行情数据。
- 增加机构类型下拉框。
- 补充 mock 机构类型字段。
- 按选中机构类型过滤/聚合展示正回购、逆回购与价差。

### Step 7：正/逆回购需求合并

- 合并两个需求卡片。
- 正回购保留分类。
- 逆回购只保留汇总行。
- 校验合计计算。

### Step 8：验证

- 运行 `npm run build`。
- 打开 `http://127.0.0.1:5173/` 验证 React 页面。
- 验收交互：
  - XRepo 只剩 R001 相关合约。
  - 点击 XRepo 行可翻到历史成交走势。
  - 匿名成交走势图有今日/昨日对比。
  - 点击大行价格行可翻到多日历史。
  - 机构分期限统计位于中间区，左侧入口可打开大弹窗。
  - 机构分期限统计入口的期限和指标以下拉框展示。
  - 机构分期限统计非图例点击出现下拉明细。
  - 个人 QT 行情融合正回购和逆回购，并有分机构统计下拉框。
  - 正/逆回购需求合并为一张表。

## 总 Checklist

### 设计与布局

- [x] 左侧被删除模块入口已从入口列表中移除，不占用左侧空间。
- [x] 中间被删除模块保留空位，整体网格不塌陷、不跳动。
- [x] 中间「匿名成交走势图」保留展示。
- [x] 「历史成交走势」从独立中间模块迁移到 XRepo 背面。
- [x] 正/逆回购需求合并为单张表，视觉层级清楚。
- [x] 翻转背面均有明确返回按钮。
- [x] 翻转背面点击空白区域可以返回正面。

### XRepo

- [x] XRepo 只展示合约名包含 `R001` 的数据。
- [x] `R001-mini`、`DFR001-mini`、`CDR001-mini` 等被保留。
- [x] `R007`、`R014`、`R021`、`R028` 等非 R001 合约不展示。
- [x] XRepo 摘要、表格、图表、筛选项口径一致。
- [x] 点击 XRepo 行后，背面历史成交走势默认使用当前行合约作为参数。
- [x] XRepo 背面支持返回正面。

### 匿名成交走势

- [x] 匿名成交走势图保留在中间区域。
- [x] 今日走势与昨日走势同时展示。
- [x] 今日线与昨日线视觉上可区分。
- [x] tooltip 同时展示今日/昨日数据。
- [x] 图表不因昨日线叠加导致信息过载。

### 大行价格

- [x] 大行价格表格每一行可点击。
- [x] 点击行后翻转到该银行多日历史。
- [x] 背面包含「大行定价走势」图。
- [x] 背面包含「大行定价与加权价差」图。
- [x] 背面图表 tooltip 可读。
- [x] 背面支持空白区域点击返回。
- [x] 背面支持返回按钮返回。

### 机构分期限统计

- [x] 机构分期限统计移动到中间行情矩阵区。
- [x] 左侧机构分期限统计入口保留为轻量入口。
- [x] 左侧入口中的期限使用下拉框。
- [x] 左侧入口中的指标使用下拉框。
- [x] 点击入口或展开按钮打开机构分期限统计大弹窗。
- [x] 大图接近参考图的信息密度和控件结构。
- [x] 指标选择包含加权利率、交易额、回购余额、跨月金额、净融入金额。
- [x] 机构类型通过 checkbox 控制。
- [x] 期限通过 checkbox 控制。
- [x] 正回购/逆回购可切换。
- [x] 折线图/柱状图可切换。
- [x] 期限/机构维度可切换。
- [x] 图例点击仍用于筛选或高亮。
- [x] 非图例项点击改为下拉展开明细。

### 个人 QT 行情

- [x] 个人 QT 行情融合正回购和逆回购数据。
- [x] 增加「分机构统计」下拉框。
- [x] 下拉选项覆盖主要机构类型。
- [x] 选择机构类型后，正回购和逆回购数据同步按机构类型过滤或聚合。
- [x] 展示正回购 QT 行情、逆回购 QT 行情、正逆回购价差/利差。
- [x] 展示样本量或覆盖机构数。
- [x] mock 数据补齐机构类型字段。

### 正/逆回购需求

- [x] 正回购需求保留押券分类明细。
- [x] 正回购需求保留合计行。
- [x] 逆回购需求只保留汇总行。
- [x] 单元格格式统一为 `需求量 / 已完成量`。
- [x] R001、R007、合计列计算准确。

### 验证

- [x] `npm run build` 通过。
- [x] 本地 React 页面 `http://127.0.0.1:5173/` 可打开。
- [x] 浏览器无明显运行时报错。
- [x] 关键交互在桌面视口验证通过。
- [x] 文案、表头、单位和 tooltip 口径一致。

## 里程碑待办

### M1：布局与入口口径收敛

目标：先把页面结构调对，避免后续功能建立在错误入口和错误布局上。

待办：

- [x] 梳理 `MODULE_ENTRIES` / 左侧入口配置，标记需要删除的入口。
- [x] 删除左侧「行情摘要今日预览」「DR007 / 资金情绪」「历史成交趋势」「匿名成交走势图」「个人 & 机构」入口。
- [x] 中间区域为被删除模块保留空位。
- [x] 保留中间「匿名成交走势图」真实图表。
- [x] 确认删除入口后左侧栏高度、间距、hover 状态正常。

验收：

- 左侧不再出现上述入口。
- 中间布局仍稳定。
- 匿名成交走势图仍可见。

### M2：XRepo R001 过滤与背面历史成交走势

目标：XRepo 只看 R001 相关合约，并承载历史成交走势。

待办：

- [x] 定义 XRepo 合约过滤函数，规则为合约名包含 `R001`。
- [x] 替换 XRepo 表格、摘要、图表的数据源过滤逻辑。
- [x] 确保 `R001-mini` 等包含 R001 的合约保留。
- [x] 增加 XRepo 行点击状态 `flippedXrepoContract`。
- [x] 将历史成交走势组件接入 XRepo 背面。
- [x] 背面默认读取点击行合约。
- [x] 增加背面返回按钮。
- [x] 增加背面空白区域点击返回。

验收：

- XRepo 只出现 R001 相关合约。
- 点击任一 XRepo 行后翻到对应合约历史成交走势。
- 返回按钮和空白区域点击都能翻回正面。

### M3：匿名成交走势昨日对比

目标：在现有匿名成交走势图上叠加昨日折线。

待办：

- [x] 补充昨日匿名成交 mock 序列。
- [x] 今日走势使用主色实线。
- [x] 昨日走势使用虚线或低饱和度折线。
- [x] tooltip 增加今日/昨日两组值。
- [x] 图例增加今日/昨日说明。
- [x] 调整图表层级，避免昨日线遮挡今日主线。

验收：

- 匿名成交走势图同时显示今日与昨日。
- 悬浮时能看到两组数据。
- 图表阅读优先级清晰。

### M4：大行价格翻转历史图

目标：大行价格表格支持按银行查看多日历史。

待办：

- [x] 增加银行历史 mock 数据结构。
- [x] 大行价格表格行增加点击态。
- [x] 增加状态 `flippedBankId`。
- [x] 实现「大行定价走势」背面图。
- [x] 实现「大行定价与加权价差」背面图。
- [x] 增加背面右上角返回按钮。
- [x] 增加背面空白区域点击返回。
- [x] 防止点击图表 tooltip、按钮时误触空白返回。

验收：

- 点击每一行银行都能进入对应历史视图。
- 两张参考图对应的信息都能表达。
- 可稳定返回正面。

### M5：机构分期限统计中间化与弹窗

目标：把机构分期限统计移动到中间区，同时把左侧入口改成期限/指标下拉 + 大弹窗入口。

待办：

- [x] 将机构分期限统计模块移动到中间行情矩阵区。
- [x] 中间区默认展示压缩版统计图或统计摘要。
- [x] 左侧机构分期限统计入口保留。
- [x] 左侧入口期限改为下拉框。
- [x] 左侧入口指标改为下拉框。
- [x] 入口下拉选择同步为大弹窗默认参数。
- [x] 增加入口点击或展开按钮，打开大弹窗。
- [x] 设计顶部指标 radio 组。
- [x] 增加机构类型 checkbox 组。
- [x] 增加期限 checkbox 组。
- [x] 增加正回购/逆回购切换。
- [x] 增加折线图/柱状图切换。
- [x] 增加期限/机构维度切换。
- [x] 默认实现柱状堆叠图。
- [x] 保留图例点击筛选/高亮。
- [x] 非图例项点击改为下拉明细。
- [x] 明细下拉展示机构、期限、指标值、更新时间等字段。

验收：

- 机构分期限统计出现在中间行情矩阵区。
- 左侧入口不再直接铺大图，只作为轻量入口。
- 左侧入口期限和指标均为下拉框。
- 点击入口可打开大弹窗。
- 控件结构接近参考图。
- 图例点击逻辑不被破坏。
- 非图例点击出现下拉明细，不直接切换图表。

### M6：个人 QT 行情融合正回购和逆回购

目标：个人 QT 行情融合正回购和逆回购，支持按机构类型查看两侧行情、差异和汇总。

待办：

- [x] 明确个人 QT 正回购与逆回购行情的数据结构。
- [x] 增加正回购 QT mock 数据。
- [x] 增加逆回购 QT mock 数据。
- [x] 增加正回购与逆回购价差/利差计算。
- [x] 在个人 QT 行情顶部增加「分机构统计」下拉框。
- [x] 定义机构类型枚举。
- [x] mock 数据补充机构类型字段。
- [x] 下拉选择后同步过滤或聚合正回购、逆回购、价差数据。
- [x] 默认选项为「全部机构」。
- [x] 空结果时保留表头并展示空状态。

验收：

- 个人 QT 行情卡片中可以看到正回购与逆回购两类口径。
- 下拉框可用。
- 切换机构类型后正回购、逆回购、价差数据范围随之变化。
- 无数据时页面不塌陷。

### M7：正/逆回购需求合并

目标：将两个需求卡片合并为一张表。

待办：

- [x] 合并正回购需求与逆回购需求的数据计算。
- [x] 正回购保留利率地方、存单商金、信用分类。
- [x] 正回购增加合计行。
- [x] 逆回购只保留汇总行。
- [x] 合计列按 R001 + R007 计算。
- [x] 单元格统一格式为 `需求量 / 已完成量`。
- [x] 删除旧的逆回购独立卡片渲染。

验收：

- 页面只出现一张正/逆回购需求表。
- 正回购明细完整。
- 逆回购只有汇总行。
- 数值合计正确。

### M8：联调与验收

目标：完成构建、浏览器验证和交互回归。

待办：

- [x] 运行 `npm run build`。
- [x] 修复构建错误和类型错误。
- [x] 启动 React 本地服务。
- [x] 在 `http://127.0.0.1:5173/` 验证页面。
- [x] 验证 XRepo 翻转。
- [x] 验证大行价格翻转。
- [x] 验证匿名成交昨日对比。
- [x] 验证机构分期限统计中间模块。
- [x] 验证机构分期限统计入口期限/指标下拉。
- [x] 验证机构分期限统计大弹窗。
- [x] 验证机构分期限统计下拉明细。
- [x] 验证个人 QT 融合正回购和逆回购。
- [x] 验证个人 QT 机构下拉。
- [x] 验证正/逆回购需求合并表。
- [x] 检查浏览器控制台错误。

验收：

- 构建通过。
- 核心功能全部可操作。
- 页面无明显错位、遮挡、文字溢出。
- 已知未完成项记录到文档或 TODO。

## 整页线框图

### 默认正面状态

```text
┌──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ 顶部状态栏                                                                                                                   │
│ 资金实时行情看板  |  交易提示滚动文案  |  DR007 2.15%  |  资金情绪 51 / 47 / 50 / 49  |  平衡                                │
├───────────────────────┬───────────────────────────────────────────────┬──────────────────────────────────────────────────────┤
│ 左侧：行情摘要          │ 中间：行情矩阵区                                │ 右侧：非银报价区                                      │
│ 宽度约 16%              │ 宽度约 34%                                      │ 宽度约 50%                                            │
│                       │                                               │                                                      │
│ ┌───────────────────┐ │ ┌──────────────────────┐ ┌────────────────────┐ │ ┌──────────────────────────────────────────────────┐ │
│ │ 今日概览           │ │ │ 正/逆回购需求合并表      │ │ 个人 QT 行情          │ │ │ 非银报价                                            │ │
│ │ DR007 / 情绪 / 成交 │ │ │ 正回购分类 + 逆回购合计  │ │ 正回购 + 逆回购融合   │ │ │ 顶部筛选：全部 / R001 / R007 / R014 / R021 / R028     │ │
│ └───────────────────┘ │ │ R001 / R007 / 合计        │ │ 分机构统计下拉框       │ │ │ 金额 / 利率 / 账户要求 / 质押要求                     │ │
│                       │ └──────────────────────┘ └────────────────────┘ │ └──────────────────────────────────────────────────┘ │
│ ┌───────────────────┐ │                                               │                                                      │
│ │ 今天大行价格        │ │ ┌──────────────────────┐ ┌────────────────────┐ │ ┌──────────────────────────────────────────────────┐ │
│ │ 行点击进入大行翻转图 │ │ │ 机构分期限统计          │ │ 匿名成交走势图        │ │ │ 我方融券                                            │ │
│ └───────────────────┘ │ │ │ 中间默认压缩统计图       │ │ 中间继续保留          │ │ │ 分组：利率地方 / 存单商金 / 信用                     │ │
│                       │ │ │ 点击可打开大弹窗         │ │ 今日 + 昨日对比折线    │ │ │ 行操作：发送                                         │ │
│ ┌───────────────────┐ │ └──────────────────────┘ └────────────────────┘ │ └──────────────────────────────────────────────────┘ │
│ │ XRepo              │ │                                               │                                                      │
│ │ 只保留 R001 相关合约 │ │ ┌──────────────────────┐ ┌────────────────────┐ │ ┌──────────────────────────────────────────────────┐ │
│ │ R001 / R001-mini    │ │ │ 历史成交走势空位        │ │ 删除模块空位          │ │ │ 我方融出                                            │ │
│ │ 行点击翻转到历史走势 │ │ │ XRepo 背面复用此图能力  │ │ 今日预览/情绪/个人机构 │ │ │ 分组：利率地方 / 存单商金 / 信用                     │ │
│ └───────────────────┘ │ │ │ 正面独立历史模块不展示    │ │ 删除后保留空位        │ │ │ 行操作：发送                                         │ │
│                       │ └──────────────────────┘ └────────────────────┘ │ └──────────────────────────────────────────────────┘ │
│ ┌───────────────────┐ │                                               │                                                      │
│ │ 交易所回购          │ │ ┌──────────────────────────────────────────────┐ │                                                      │
│ └───────────────────┘ │ │ │ 中间保留空位 / 后续扩展区                         │ │                                                      │
│                       │ │ │ 删除模块空位、弹窗回填状态或辅助统计摘要            │ │                                                      │
│ ┌───────────────────┐ │ │ │                                                  │ │                                                      │
│ │ NCD               │ │ └──────────────────────────────────────────────┘ │                                                      │
│ └───────────────────┘ │                                               │                                                      │
│                       │ ┌──────────────────────────────────────────────┐ │                                                      │
│ ┌───────────────────┐ │ │ 删除模块空位区                                  │ │                                                      │
│ │ 机构分期限统计入口   │ │ │ 今日预览 / DR007资金情绪 / 个人&机构等删除后占位    │ │                                                      │
│ │ 期限▼ 指标▼ 打开大图 │ │ └──────────────────────────────────────────────┘ │                                                      │
│ └───────────────────┘ │                                               │                                                      │
└───────────────────────┴───────────────────────────────────────────────┴──────────────────────────────────────────────────────┘
```

### 中间行情矩阵区放大线框

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 行情矩阵区                                                                    │
├──────────────────────────────────┬───────────────────────────────────────────┤
│ 正/逆回购需求合并表                 │ 个人 QT 行情                               │
│                                  │ [分机构统计 ▼] [正回购] [逆回购] [汇总]       │
│ 正回购需求 | R001 | R007 | 合计      │                                           │
│ 利率地方   | ...  | ...  | ...      │ 今日成交变化 / 日内折线                    │
│ 存单商金   | ...  | ...  | ...      │                                           │
│ 信用       | ...  | ...  | ...      │                                           │
│ 合计       | ...  | ...  | ...      │                                           │
│ 逆回购需求 | ...  | ...  | ...      │                                           │
├──────────────────────────────────┼───────────────────────────────────────────┤
│ 机构分期限统计                         │ 匿名成交走势图                              │
│ 中间默认展示压缩版统计图/摘要             │ 产品：R001 ▼  叠加品种：不叠加 ▼             │
│ 点击卡片或展开按钮打开大弹窗              │ 今日匿名成交折线 + 昨日对比折线 + tooltip      │
├──────────────────────────────────┼───────────────────────────────────────────┤
│ 历史成交走势 / XRepo 背面复用          │ 删除模块空位区                                │
│ 正面可留空                           │ 今日预览 / DR007资金情绪 / 个人&机构等占位      │
│ 点击 XRepo 行后，背面用该区域图表能力    │ 删除模块只占位，不放交互控件                    │
│ 默认参数 = 被点击合约名                 │ 后续可用于辅助统计摘要                         │
└──────────────────────────────────┴───────────────────────────────────────────┘
```

### 机构分期限统计入口与大弹窗

```text
左侧轻量入口
┌──────────────────────────────┐
│ 机构分期限统计                 │
│                              │
│ 期限    [R001 ▼]              │
│ 指标    [回购余额 ▼]           │
│                              │
│ [打开大图]                     │
└──────────────────────────────┘

点击入口 / 打开大图后
┌──────────────────────────────────────────────────────────────────────────────┐
│ 机构分期限统计大图                                      [导出] [关闭]        │
│ 默认参数：期限 = 左侧入口期限；指标 = 左侧入口指标                              │
├──────────────────────────────────────────────────────────────────────────────┤
│ 指标：○ 加权利率  ○ 交易额  ● 回购余额  ○ 跨月金额  ○ 净融入金额                │
│ 机构：☑ 大型银行 ☑ 大型商业/政策性银行 ☑ 股份制商业银行 ☑ 中小型银行 ...        │
│ 期限：☑ R001 ☑ R007 ☑ R014 ☑ R021 ☑ R1M ☑ R2M ☑ R3M ☑ R4M ...                │
│ 方向：● 正回购 ○ 逆回购    图形：○ 折线图 ● 柱状图    维度：● 期限 ○ 机构       │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│                        大尺寸堆叠柱状图 / 折线图                               │
│                                                                              │
│ 图例点击：筛选/高亮                                                            │
│ 非图例点击：下拉展开明细                                                        │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 大行价格翻转背面

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 大行价格背面：{选中银行} 多日历史                                  [返回]    │
│ 点击空白区域也可翻回正面                                                       │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│ ┌──────────────────────────────────┐ ┌──────────────────────────────────┐   │
│ │ 大行定价走势                      │ │ 大行定价与加权价差                │   │
│ │                                  │ │                                  │   │
│ │ 出给非银价格 折线                 │ │ 上半区：给银行价差 BP 柱状        │   │
│ │ 出给银行价格 折线                 │ │ 下半区：给非银价差 BP 柱状        │   │
│ │ 非银-银行价差 BP 柱状             │ │                                  │   │
│ │ 双 Y 轴 + tooltip                 │ │ tooltip                           │   │
│ └──────────────────────────────────┘ └──────────────────────────────────┘   │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

### XRepo 翻转背面

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ XRepo 背面：历史成交走势 - {选中合约}                              [返回]    │
│ 点击空白区域也可翻回正面                                                       │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│ ┌────────────────────────────────────────────────────────────────────────┐   │
│ │ 历史成交走势                                                           │   │
│ │ 默认参数 = 正面点击行的合约名                                            │   │
│ │ 支持 R001 / R001-mini / DFR001-mini / CDR001-mini 等包含 R001 的合约      │   │
│ │ 折线：利率走势                                                          │   │
│ │ 柱状：成交量                                                            │   │
│ │ tooltip：日期 / 合约 / 利率 / 成交量                                      │   │
│ └────────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 正/逆回购需求合并表

```text
┌────────────────────────────────────────────────────────────┐
│ 正/逆回购需求                                      [R001] [R007] │
│ 本方需求 · 押券 × 期限 · 单位：亿                              │
├────────────────────────────────────────────────────────────┤
│  ┌────────────┬──────────────┬──────────────┬──────────────┐ │
│  │ 正回购需求   │ R001          │ R007          │ 合计          │ │
│  ├────────────┼──────────────┼──────────────┼──────────────┤ │
│  │ 利率地方     │ 40.6 / 31.2  │ 16.4 / 13.1  │ 57.0 / 44.3  │ │
│  │ 存单商金     │ 0.0 / 0.0    │ 25.3 / 18.4  │ 25.3 / 18.4  │ │
│  │ 信用        │ 9.2 / 7.1    │ 15.7 / 15.7  │ 24.9 / 22.8  │ │
│  ├────────────┼──────────────┼──────────────┼──────────────┤ │
│  │ 合计        │ 49.8 / 38.3  │ 57.4 / 47.2  │ 107.2 / 85.5 │ │
│  ├────────────┼──────────────┼──────────────┼──────────────┤ │
│  │ 逆回购需求   │ 35.7 / 29.4  │ 48.3 / 35.1  │ 84.0 / 64.5  │ │
│  └────────────┴──────────────┴──────────────┴──────────────┘ │
└────────────────────────────────────────────────────────────┘
```
