# TDX Theme

本目录记录通达信证券终端风格的本地主题建议，用于把当前界面从偏蓝灰的看板风格，逐步对齐到深色交易终端风格。

## 文件

- `DESIGN.md`: 主题方向、组件规则、开发顺序和验收标准。
- `tokens.css`: 可迁移到 `src/styles/theme.css` 的主题 token 草案。

## 使用方式

开发时先参考 `DESIGN.md` 的组件规则，再按 `tokens.css` 替换或映射现有 `--tk-*` token。建议按全局背景、顶部导航、表格、左侧筛选、标签状态、图表弹窗的顺序逐步落地。
